printf("------------- read(portico77)----------------\n");
portico77:=proc(Ls, combinacionesX, variacionesP, variacionesq, variacionesM,
		  VariacionesM,
		  escal, escalP, escalq, escalM, posiPRelativa 
                  )
    global CODIGO;
    local Lvol, Lv, Lc, nudos, ccont, conex, vigas, apoyo, s;
    
    #  1           2
    #----------------------
    #     /               ^
    #    /
    #   /3
    #  /
    # /

 
    printf("-------------     (portico77)----------------\n");

    CODIGO:=P77;

    s :=     portico77Calculo(Ls, combinacionesX, variacionesP, variacionesq,
      	     			variacionesM, VariacionesM);

             portico10Psfrag( Ls, combinacionesX, variacionesP, variacionesq,
	     		    	variacionesM, VariacionesM,
	                         s);
printf("aqui tendría que empezar portico70Tex\n");
             portico70Tex(    Ls, combinacionesX, variacionesP, variacionesq,
	     		    	variacionesM, VariacionesM,
	                         s);

 

    #----------- Lo último es el dibujo para actualizar la variable global FICH
    Fich();
    print(display(
             portico77Dibujo(Ls, combinacionesX, variacionesP, variacionesq,
	     		       variacionesM, VariacionesM,
	       	               escal, escalP, escalq, escalM, posiPRelativa)));
				 
    printf("------------- fin (portico77)----------------\n");
end;
#--------------------
#print(portico77); # Para fijar variables locales
printf("-------------     (portico77)----------------\n");






